{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.10"
  },
  "deploy": {
    "startCommand": "DISABLE_VISION=1 gunicorn --chdir backend --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 180 wsgi:app"
  }
}
