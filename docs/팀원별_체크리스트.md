# 팀원별 작업 체크리스트

> 기준 문서: [개선과제_영역별정리.md](./개선과제_영역별정리.md)
> 마지막 업데이트: 2026-07-02
>
> 🔴 = 시제품 공개 전 필수(P0) · 🟡 = 안정적인 데모를 위해 필요(P1) · 🟢 = 여유 있으면(P2)
> 완료 시 `- [ ]` → `- [x]` 로 체크 후 커밋

---

## 👤 김동완 — 서버 · 백엔드

### 🔴 P0 (데모 전 반드시)

- [x] **`session/store.py` 전역 상태 분리** — `branch_id`별 딕셔너리로 분리 완료 (최영수, 2026-06-24)
- [x] **`server.js` roleToSocketId 다대다 매칭 수정** — `branchId:role` 키 + room 기반으로 지점별 완전 분리 완료 (최영수, 2026-06-24)
- [ ] **server.js role 위조 차단 + `--allow-unauthenticated` 개선** — `role`/`branchId`/payload 검증 및 `SIGNALING_TOKEN` 기반 등록 차단 훅 추가 완료(2026-07-02). 운영 환경에 `SIGNALING_TOKEN`, `VITE_SIGNALING_TOKEN`, 필요 시 `SIGNALING_ALLOWED_ORIGINS` 설정 필요. Cloud Run `--allow-unauthenticated` 자체는 아직 남아있음 (`server/server.js`, `deploy-signaling.yml`)
- [x] **`railway_wsgi.py` 삭제** — 파일 삭제 완료 (2026-06-24)
- [x] **기본 관리자 계정 및 Flask secret key 환경변수 강제화** — `FLASK_SECRET_KEY`, `ADMIN_PASSWORD_HASH` 미설정 시 서버 시작 차단으로 수정 완료 (`backend/config.py`, 2026-06-24)
- [x] **`/api/summary`, `/api/logs` 인증 미들웨어 적용** — `@login_required` 추가 완료 (`backend/summary/routes.py`, `backend/logs/routes.py`, 2026-06-24)

### 🟡 P1 (데모 안정성)

- [x] **GCP VM 백엔드 배포 자동화** — GitHub Actions 방식은 아니지만 VM pull 방식 cron으로 완료. `youngsu0930` 커밋 `9069dfd`(2026-06-24, "백엔드 VM 자동배포(pull 방식) 스크립트·문서 추가") 기준, VM이 1분마다 `origin/main`을 감시해 ff-merge 후 `ksl-backend` 서비스를 재시작. 커밋 메시지에 VM 설치·테스트 완료(옛 커밋→자동 따라잡기→재시작→로그인 정상 확인) 명시 (`deploy/vm-autodeploy.sh`, `deploy/README.md`)
- [x] **쿠키 `Secure`/`HttpOnly`/`SameSite` 설정 + 세션 만료 갱신** — Flask config 반영 및 요청 시 세션 만료 갱신 완료 (`backend/config.py`, `backend/app.py`, `backend/auth/routes.py`, 2026-07-02)
- [x] **비밀번호 SHA-256 → Argon2/bcrypt 전환 + 로그인 실패 횟수 제한** — 신규 지점 계정은 Werkzeug `scrypt` 해시 사용, 기존 SHA-256은 호환 검증 유지, 로그인 실패 제한 추가 완료 (`backend/db.py`, `backend/auth/routes.py`, 2026-07-02)
- [x] **Anthropic API 호출 timeout 추가** — `timeout=30.0` 추가 완료 (`backend/summary/ai_client.py`, 2026-06-24)

### 🟢 P2 (여유 있으면)

- [x] `backend/main.py` 죽은 코드 제거 (`print("Hello from backend!")`) 완료 (2026-07-02)
- [ ] `pyproject.toml`(uv) vs `requirements.txt` 의존성 이원화 → 하나로 통일
- [x] `server/` Node.js 최소 테스트 추가 완료 (`node --test`, 2026-07-02)

---

## 👤 윤가연 — 프론트 · 수어통역사 섭외/테스트 조율

### 🔴 P0 (데모 전 반드시)

- [x] **카카오 토큰 재발급 및 환경변수 재설정** — GCP VM `.env` 업데이트 완료 (2026-06-24)
- [ ] **수어통역사 섭외 및 일정 확정** — **2026-07-02(목) 섭외 목표** (기존 6-30에서 변경). 섭외 후 1주 내 실제 수어 촬영 + 웹캠 인식 테스트 완료
- [x] **`web/App.tsx` localStorage 인증 우회 제거** — `youngsu0930` 커밋 `a5f9235`(2026-06-24)로 서버 세션(`/api/auth/status`) 기준 인증 검증과 지점 복원 구현. 2026-07-02에 남아 있던 localStorage 인증 플래그 초기 렌더 의존까지 제거 보완 완료 (`web/src/App.tsx`)
- [x] **`web/src/pages/KakaoCallback.tsx` 토큰 저장 방식 변경** — access/refresh token은 서버 세션에 저장하고, 프론트에는 완료 플래그만 저장하도록 변경 완료 (`web/src/pages/KakaoCallback.tsx`, `backend/inference/routes.py`, 2026-07-02)

### 🟡 P1 (데모 안정성)

- [ ] **분절(세그멘테이션) 경계 동작 확인** — 손이 아래로 내려갈 때 등 동작 경계에서 분절이 제대로 끊기는지 수어통역사 테스트 때 반드시 확인. 안 끊기면 수어 인식 전체가 안 되는 것 (`backend/inference/predictor.py`)
- [ ] **"안녕하세요" 등 특정 표현 오인식 재현 및 원인 특정** — 모델 문제인지 / threshold 문제인지 / mediapipe 입력 문제인지 분리해서 확인. 수어통역사 테스트 때 같이 진행
- [x] **`web/src/hooks/useSignLanguage.ts` console.log 제거** — 수어 인식 결과 콘솔 노출 제거 완료 (2026-06-24)
- [x] **`web/src/mediapipeClient.ts` client 모드 구현 또는 차단** — `youngsu0930` 커밋 `d382c14`(2026-06-26)로 `@mediapipe/tasks-vision` 기반 클라이언트 MediaPipe 구현 완료. 이어 `67da565`로 기본 모드 client 전환, `4368567`로 오버레이 그리기와 네트워크 전송 분리 완료 (`web/src/mediapipeClient.ts`, `web/src/hooks/useSignLanguage.ts`)
- [ ] **정확도 수치 통일** — 코드 기준(시나리오 서브셋 평균 ~87%, 최저 40%) vs 회의 공유치(AIHub validation 기준 ~96%) 측정 기준이 다름. 수어통역사 테스트 때 동일 기준으로 재측정 후 하나로 정리

### 🟢 P2 (여유 있으면)

- [ ] `useSignLanguage.ts` 1,600줄 → 카메라/분절/API/데모 훅으로 분리 리팩터링
- [ ] 카메라 권한 거부 시 에러 메시지 분기 (현재 "손을 보여주세요"만 표시)

---

## 👤 최영수 — DB · 프론트 · 수어통역사 테스트

### 🔴 P0 (데모 전 반드시)

- [x] **`db.py` fail-open 암호화 → fail-closed** — `youngsu0930` 커밋 `cd1c06f`(2026-06-25)로 `DB_ENCRYPTION_KEY` 미설정/암호화 실패 시 예외 발생, 평문 저장 차단 완료. 커밋 메시지에 키 있음/없음 검증 완료 명시 (`backend/db.py`)
- [ ] **`web/vercel.json` 고정 IP 제거 → 도메인 연결** — `http://34.158.217.119:8080` 하드코딩 상태. VM 재기동/재생성 시 IP 바뀌면 바로 503. GCP 고정 IP 예약 또는 Cloud DNS 도메인 연결로 IP 의존 제거 (`web/vercel.json`)

### 🟡 P1 (데모 안정성)

- [ ] **TURN 서버 설정 단일화** — `youngsu0930` 커밋 `3380a49`(2026-06-25)로 `web` 내부의 `AgentDashboard`/`CitizenKiosk`는 `web/src/hooks/webrtcConfig.ts`를 쓰도록 단일화 완료. 아직 `web-kiosk/src/hooks/webrtcConfig.ts`가 별도 중복 정의라 `web`/`web-kiosk` 공용 설정까지는 미완료.
- [x] **`purge_old_data()` 자동 실행 스케줄러 추가** — Flask 앱 시작 시 retention worker가 1일 주기로 호출하도록 추가 (`backend/db.py`, `backend/app.py`, 2026-07-02)
- [x] **`/api/correction` 호출하는 프론트 UI 연결** — `youngsu0930` 커밋 `5ae81b2`(2026-06-25)로 상담원 대시보드 "인식 수정" UI와 POST `/api/correction` 연결 완료. 2026-07-02에 해당 API 인증 미들웨어 보완 (`web/src/pages/AgentDashboard.tsx`, `backend/inference/routes.py`)
- [ ] **Vercel↔VM 평문 HTTP** — 지금은 테스트 데이터라 급하진 않지만, 실 PII 투입 전에 GCP 로드밸런서 또는 Cloud Run 전환으로 HTTPS 적용 필요 (`web/vercel.json`)
- [ ] **랜드마크 추적 실패 원인 분석** — mediapipe가 손/포즈를 못 잡는 케이스. threshold 문제인지 입력 해상도/조명 문제인지 확인. 수어통역사 테스트 때 윤가연과 함께 진행

### 🟢 P2 (여유 있으면)

- [ ] DB 마이그레이션 도구 Alembic 도입 (테이블 5개로 늘어난 지금이 적기)
- [ ] 지점 간 집계가 필요해질 때를 위한 중앙 동기화 방안 설계 메모
- [ ] Vitest/React Testing Library 도입

---

## 📌 공통 / 수어통역사 테스트 (윤가연 + 최영수)

> 테스트 일정: **2026-06-30(화) 섭외 → 1주 내 촬영+테스트 완료 → 이후 2주 보완**
> 최신 일정: **2026-07-02(목) 섭외 목표**로 변경됨. 섭외 후 1주 내 촬영+테스트, 이후 2주 보완.

## 📌 배포 근거 메모 (git log 기준)

- [x] **프론트 Vercel → GCP VM 프록시 연결** — `youngsu0930` 커밋 `7625bc7`(2026-06-23, "프론트(Vercel) /api 요청을 GCP 백엔드 VM으로 프록시 연결")로 `web/vercel.json` 추가/연결 확인. 단 고정 IP + HTTP 문제는 최영수 P0/P1 항목으로 별도 유지.
- [x] **소켓 서버 Cloud Run 자동배포** — `youngsu0930` 커밋 `68a0932`(2026-06-23, "소켓 서버 자동배포 워크플로 추가 (GitHub Actions + WIF)")로 `.github/workflows/deploy-signaling.yml` 확인. 대상 서비스 `ksl-signaling`, region `asia-northeast3`.
- [x] **백엔드 GCP VM 자동배포** — `youngsu0930` 커밋 `9069dfd`(2026-06-24, "백엔드 VM 자동배포(pull 방식) 스크립트·문서 추가")로 `deploy/vm-autodeploy.sh`, `deploy/README.md` 추가 확인. README도 `main` 머지 시 프론트/시그널링/백엔드 3개 구성요소 모두 자동배포라고 정리되어 있음.

## 📌 최영수 작업 반영 메모 (git log 기준)

- [x] **DB/개인정보 저장 구조** — `1e5508e`(2026-06-22, SQLite 전환 + 비식별 인식 로그), `34c7215`(2026-06-22, 상담/오인식/대화 테이블), `88d86b2`(2026-06-23, KST 타임스탬프), `cd1c06f`(2026-06-25, 암호화 fail-closed) 확인.
- [x] **지점 분리/멀티테넌시** — `51dbd9c`, `80a958a`, `adb4f86`, `a5f9235`, `6d5f453`, `0922def`(모두 2026-06-24, `youngsu0930`)로 DB/세션/소켓/프론트/마이그레이션/지점명 배지 반영 확인.
- [x] **배포 인프라** — `7625bc7`, `ebffa4a`, `997d2bd`, `68a0932`, `9069dfd`로 Vercel↔GCP VM 프록시, Cloud Run 준비/자동배포, GCP VM pull 방식 자동배포 확인.
- [x] **수어 인식 실환경 최적화** — `d382c14`, `67da565`, `4368567`, `a719833`, `14f7cdf`(2026-06-26, `youngsu0930`)로 클라이언트 MediaPipe, 기본 client 모드, 오버레이/네트워크 분리, stale 응답 처리, 문서화 확인.
- [x] **상담원 교정 UI/TURN 일부 단일화** — `5ae81b2`(2026-06-25, 인식 수정 UI), `3380a49`(2026-06-25, `web` 내부 TURN/ICE 설정 단일화) 확인. 단 `web-kiosk`와의 공용 TURN 설정은 아직 별도 TODO.

- [ ] 수어통역사 섭외 확정 및 일정 조율 
- [ ] 테스트 시나리오 준비: "복지카드 분실 신고" 전체 흐름 대본 작성
- [ ] 분절 경계 테스트: 손 내려갈 때 / 동작 멈출 때 분절 제대로 끊기는지 촬영
- [ ] 오인식 케이스 기록: "안녕하세요" 포함 5개 이상 표현 테스트, 인식 결과 스크린샷
- [ ] **모델 문제 vs 입력 문제 구분**: 동일 수어를 여러 번 반복해서 재현 가능한지 확인
- [ ] 테스트 결과 정리해서 팀 공유 (ML 섹션 정확도 수치 통일에 반영)
