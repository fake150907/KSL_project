# 팀원별 작업 체크리스트

> 기준 문서: [개선과제_영역별정리.md](./개선과제_영역별정리.md)
> 마지막 업데이트: 2026-06-24
>
> 🔴 = 시제품 공개 전 필수(P0) · 🟡 = 안정적인 데모를 위해 필요(P1) · 🟢 = 여유 있으면(P2)
> 완료 시 `- [ ]` → `- [x]` 로 체크 후 커밋

---

## 👤 김동완 — 서버 · 백엔드

### 🔴 P0 (데모 전 반드시)

- [ ] **`session/store.py` 전역 상태 분리** — `citizen_session`/`chat_messages` 딕셔너리가 전역 하나라 민원인 2명 동시 접속 시 데이터 섞임. `consultation_id` 키로 상담별로 분리 (`backend/session/store.py`)
- [ ] **`server.js` roleToSocketId 다대다 매칭 수정** — `roleToSocketId[role] = socket.id`가 마지막 접속자로 덮어써짐. Socket.IO room 또는 namespace 기반으로 지점·상담 ID별 분리 (`server/server.js:25,51-53`)
- [ ] **server.js role 위조 차단 + `--allow-unauthenticated` 개선** — 현재 클라이언트가 보낸 `role` 값을 검증 없이 신뢰. JWT 또는 일회용 상담 토큰 검증 추가 필요. Cloud Run이 인터넷에 인증 없이 열려있으므로 우선순위 높음 (`server/server.js`, `deploy-signaling.yml`)
- [x] **`railway_wsgi.py` 삭제** — 파일 삭제 완료 (2026-06-24)
- [x] **기본 관리자 계정 및 Flask secret key 환경변수 강제화** — `FLASK_SECRET_KEY`, `ADMIN_PASSWORD_HASH` 미설정 시 서버 시작 차단으로 수정 완료 (`backend/config.py`, 2026-06-24)
- [x] **`/api/summary`, `/api/logs` 인증 미들웨어 적용** — `@login_required` 추가 완료 (`backend/summary/routes.py`, `backend/logs/routes.py`, 2026-06-24)

### 🟡 P1 (데모 안정성)

- [ ] **GCP VM 백엔드 배포 자동화** — 소켓 서버는 `deploy-signaling.yml`로 CI 자동배포 있는데 VM은 없음. `main`의 `backend/` 변경 시 자동 배포하는 워크플로 추가 (`.github/workflows/`)
- [ ] **쿠키 `Secure`/`HttpOnly`/`SameSite` 설정 + 세션 만료 갱신** — 현재 Flask 세션 쿠키에 보안 속성 미설정 (`backend/config.py`, `app.py`)
- [ ] **비밀번호 SHA-256 → Argon2/bcrypt 전환 + 로그인 실패 횟수 제한** (`backend/config.py`, `backend/auth/routes.py`)
- [x] **Anthropic API 호출 timeout 추가** — `timeout=30.0` 추가 완료 (`backend/summary/ai_client.py`, 2026-06-24)

### 🟢 P2 (여유 있으면)

- [ ] `backend/main.py` 죽은 코드 제거 (`print("Hello from backend!")`)
- [ ] `pyproject.toml`(uv) vs `requirements.txt` 의존성 이원화 → 하나로 통일
- [ ] `server/` Node.js 최소 테스트 추가 (현재 비어있음)

---

## 👤 윤가연 — 프론트 · 수어통역사 섭외/테스트 조율

### 🔴 P0 (데모 전 반드시)

- [x] **카카오 토큰 재발급 및 환경변수 재설정** — GCP VM `.env` 업데이트 완료 (2026-06-24)
- [x] **`web/App.tsx` localStorage 인증 우회 제거** — 마운트 시 `/api/auth/status` 서버 검증 추가, localStorage 단독 신뢰 제거 완료 (`web/src/App.tsx`, 2026-06-24)
- [ ] **`web/src/pages/KakaoCallback.tsx` 토큰 저장 방식 변경** — `localStorage.setItem('KAKAO_ACCESS_TOKEN', ...)` → httpOnly 쿠키 또는 서버 보관으로 변경 (`web/src/pages/KakaoCallback.tsx:43-45`)
- [ ] **수어통역사 섭외 및 일정 확정** — 2026-06-30(화) 섭외 목표. 섭외 후 1주 내 실제 수어 촬영 + 웹캠 인식 테스트 완료. 테스트 결과 문서화해서 팀 공유

### 🟡 P1 (데모 안정성)

- [ ] **분절(세그멘테이션) 경계 동작 확인** — 손이 아래로 내려갈 때 등 동작 경계에서 분절이 제대로 끊기는지 수어통역사 테스트 때 반드시 확인. 안 끊기면 수어 인식 전체가 안 되는 것 (`backend/inference/predictor.py`)
- [ ] **"안녕하세요" 등 특정 표현 오인식 재현 및 원인 특정** — 모델 문제인지 / threshold 문제인지 / mediapipe 입력 문제인지 분리해서 확인. 수어통역사 테스트 때 같이 진행
- [x] **`web/src/hooks/useSignLanguage.ts` console.log 제거** — 수어 인식 결과 콘솔 노출 제거 완료 (2026-06-24)
- [ ] **`web/src/pages/mediapipeClient.ts` client 모드 구현 또는 차단** — `?mp=client` 진입 시 미구현 에러로 예측 루프 죽음. 완성하거나 해당 진입점 막기
- [ ] **정확도 수치 통일** — 코드 기준(시나리오 서브셋 평균 ~87%, 최저 40%) vs 회의 공유치(AIHub validation 기준 ~96%) 측정 기준이 다름. 수어통역사 테스트 때 동일 기준으로 재측정 후 하나로 정리

### 🟢 P2 (여유 있으면)

- [ ] `useSignLanguage.ts` 1,600줄 → 카메라/분절/API/데모 훅으로 분리 리팩터링
- [ ] 카메라 권한 거부 시 에러 메시지 분기 (현재 "손을 보여주세요"만 표시)

---

## 👤 최영수 — DB · 프론트 · 수어통역사 테스트

### 🔴 P0 (데모 전 반드시)

- [ ] **`db.py` fail-open 암호화 → fail-closed** — `encrypt_value()`에서 `DB_ENCRYPTION_KEY` 미설정 시 평문 저장. 키 없으면 저장 자체를 막도록(예외 발생) 수정 (`backend/db.py`)
- [ ] **`web/vercel.json` 고정 IP 제거 → 도메인 연결** — `http://34.158.217.119:8080` 하드코딩 상태. VM 재기동/재생성 시 IP 바뀌면 바로 503. GCP 고정 IP 예약 또는 Cloud DNS 도메인 연결로 IP 의존 제거 (`web/vercel.json`)

### 🟡 P1 (데모 안정성)

- [ ] **TURN 서버 설정 단일화** — `web/src/hooks/webrtcConfig.ts`, `web/src/pages/AgentDashboard.tsx`, `web-kiosk/src/hooks/webrtcConfig.ts` 세 곳에 openrelayproject 계정이 중복 하드코딩. 공용 상수/패키지로 통합
- [ ] **`purge_old_data()` 자동 실행 스케줄러 추가** — 함수는 `db.py`에 있지만 호출 트리거 없음. Flask 앱 시작 시 APScheduler 또는 cron 연동 (`backend/db.py`, `backend/app.py`)
- [ ] **`/api/correction` 호출하는 프론트 UI 연결** — 백엔드 API는 만들어져 있음. 상담원 대시보드(`AgentDashboard.tsx`)에 "인식 결과 수정" 버튼 추가해서 연동 (`web/src/pages/AgentDashboard.tsx`, `backend/inference/routes.py`)
- [ ] **Vercel↔VM 평문 HTTP** — 지금은 테스트 데이터라 급하진 않지만, 실 PII 투입 전에 GCP 로드밸런서 또는 Cloud Run 전환으로 HTTPS 적용 필요 (`web/vercel.json`)
- [ ] **랜드마크 추적 실패 원인 분석** — mediapipe가 손/포즈를 못 잡는 케이스. threshold 문제인지 입력 해상도/조명 문제인지 확인. 수어통역사 테스트 때 윤가연과 함께 진행

### 🟢 P2 (여유 있으면)

- [ ] DB 마이그레이션 도구 Alembic 도입 (테이블 5개로 늘어난 지금이 적기)
- [ ] 지점 간 집계가 필요해질 때를 위한 중앙 동기화 방안 설계 메모
- [ ] Vitest/React Testing Library 도입

---

## 📌 공통 / 수어통역사 테스트 (윤가연 + 최영수)

> 테스트 일정: **2026-06-30(화) 섭외 → 1주 내 촬영+테스트 완료 → 이후 2주 보완**

- [ ] 수어통역사 섭외 확정 및 일정 조율 
- [ ] 테스트 시나리오 준비: "복지카드 분실 신고" 전체 흐름 대본 작성
- [ ] 분절 경계 테스트: 손 내려갈 때 / 동작 멈출 때 분절 제대로 끊기는지 촬영
- [ ] 오인식 케이스 기록: "안녕하세요" 포함 5개 이상 표현 테스트, 인식 결과 스크린샷
- [ ] **모델 문제 vs 입력 문제 구분**: 동일 수어를 여러 번 반복해서 재현 가능한지 확인
- [ ] 테스트 결과 정리해서 팀 공유 (ML 섹션 정확도 수치 통일에 반영)
